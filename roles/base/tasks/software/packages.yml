- name: apt | install | packages
  tags: packages, apt
  become: "{{ ansible_distribution != 'MacOSX' }}"
  package:
    state: present
    name:
      - awscli
      - catimg
      - cmake
      - fonts-firacode
      - fonts-powerline
      - htop
      - lynx
      - make
      - nodejs
      - npm
      - openconnect
      - powerline
      - python3
      - zsh
